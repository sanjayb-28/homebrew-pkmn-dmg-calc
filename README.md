# PKMN DMG Calc Homebrew tap

Install the latest universal macOS release with:

```sh
brew install --cask sanjayb-28/pkmn-dmg-calc/pkmn-dmg-calc
```

The app uses its signed in-app updater for subsequent releases.
